<header class="bg-gray-300 md:flex md:justify-between">
	<div class="flex justify-between items-center px-4 py-2">
		{% if custom_logo_url %}
			<a href="{{site.url}}" class="hdr-logo-lin">
				<img src="{{ custom_logo_url|resize( 'auto', 50) }}" alt="{{ site.name }}">
			</a>
		{% else %}
			<h1 class="hdr-logo" role="banner">
				<a class="hdr-logo-link font-bold" href="{{site.url}}" rel="home">{{site.name}}</a>
			</h1>
		{% endif %}
		<svg id="menuToggle" role="button" class="fill-current text-gray-800 md:hidden" viewbox="0 0 100 80" width="40" height="40">
			<rect width="100" height="20"></rect>
			<rect y="30" width="100" height="20"></rect>
			<rect y="60" width="100" height="20"></rect>
		</svg>
	</div>
	<nav id="mainMenu" class="hidden bg-blue-300 px-4 py-2 md:block" role="navigation">
		{% include "menu.twig" with {'items': menu.get_items} %}
	</nav>
	<!-- #nav -->
</header>
